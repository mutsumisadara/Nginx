<%= form_with(model: nginx_blog) do |form| %>
  <% if nginx_blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(nginx_blog.errors.count, "error") %> prohibited this nginx_blog from being saved:</h2>

      <ul>
        <% nginx_blog.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
